import React from "react";

function NavBar() {
  // update the JSX being returned!
  return
  <div>
   <nav>NavBar</nav>;
   <a href="#">Home</a>
   <a href="#">About</a>

   </div>
}

export default NavBar;
